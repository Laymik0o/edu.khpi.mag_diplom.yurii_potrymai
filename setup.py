from setuptools import setup

setup(
    name="mag_diplom",
    version="1.0",
    install_requires=[
        "numpy >= 1.18.4",
        "scipy >= 1.4.1",
        "tensorflow >= 2.5.0",
        "wfdb >= 2.2.1"
    ],
)
